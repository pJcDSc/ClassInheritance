#include <iostream>
#include <cstring>
#include "Media.h"

using namespace std;

Room::Room(char[20] title, int year) {
  this -> title = title;
  this -> year = year;
}

char* getTitle() {
  return title;
}

int getYear() {
  return year;
}
  
  
